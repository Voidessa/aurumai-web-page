# 🔍 Диагностика проблемы с формой

## ⚠️ Важно понимать:

**Бекенд URL** (`https://aurumai-web-page.vercel.app`) — это API-only проект, там НЕТ фронтенда, поэтому 404 нормально.

**Фронтенд URL** (`https://aurumai-web-page-frontend.vercel.app`) — там должна быть форма!

---

## 🔧 Шаг 1: Проверьте, что открыт правильный сайт

### ❌ НЕ открывайте:
```
https://aurumai-web-page.vercel.app
```
Это бекенд (API), там нет формы!

### ✅ Откройте:
```
https://aurumai-web-page-frontend.vercel.app
```
Это фронтенд, там есть форма!

---

## 🔧 Шаг 2: Проверьте консоль браузера на фронтенде

1. Откройте **ФРОНТЕНД**: `https://aurumai-web-page-frontend.vercel.app`
2. Нажмите **F12** (или Cmd+Option+I на Mac)
3. Перейдите во вкладку **Console**
4. Попробуйте отправить форму
5. Посмотрите, какие ошибки появились

**Что искать:**
- CORS ошибки
- 404 ошибки
- Сетевые ошибки (Network errors)
- JavaScript ошибки

---

## 🔧 Шаг 3: Проверьте Network tab

1. В DevTools перейдите во вкладку **Network**
2. Попробуйте отправить форму
3. Найдите запрос к `/api/lead`
4. Нажмите на него и посмотрите:
   - **Status:** какой статус ответа (200, 400, 500?)
   - **Response:** что в ответе от сервера
   - **Headers:** правильный ли URL бекенда

---

## 🔧 Шаг 4: Проверьте API бекенда напрямую

Откройте в браузере:
```
https://aurumai-web-page.vercel.app/api/healthz
```

**Должно вернуться:** `ok`

Если не работает — проблема в бекенде.

---

## 🔧 Шаг 5: Проверьте переменные окружения в Vercel

1. Зайдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Откройте проект **БЕКЕНДА**: `aurumai-web-page`
3. Settings → Environment Variables
4. Убедитесь, что ВСЕ переменные добавлены:
   - ✅ `CRM_TARGET=SHEETS`
   - ✅ `TELEGRAM_BOT_TOKEN`
   - ✅ `TELEGRAM_ADMIN_CHAT_ID`
   - ✅ `GOOGLE_SA_EMAIL`
   - ✅ `GOOGLE_SA_KEY` (самое важное!)
   - ✅ `GOOGLE_SHEETS_ID`

---

## 🔧 Шаг 6: Проверьте URL API в коде

Откройте файл `components/FinalCTA.tsx` и проверьте:

```typescript
const API_BASE_URL = 'https://aurumai-web-page.vercel.app';
```

Должен быть URL бекенда!

---

## 🔧 Шаг 7: Проверьте CORS

Next.js API автоматически настраивает CORS, но если есть проблемы:

1. В Network tab посмотрите на заголовки запроса
2. Должен быть заголовок `Access-Control-Allow-Origin`
3. Если его нет — проблема с CORS

---

## 🐛 Частые проблемы и решения:

### Проблема 1: "Failed to fetch" или CORS ошибка

**Причина:** Бекенд не отвечает или блокирует запрос

**Решение:**
- Проверьте, что бекенд работает: `/api/healthz`
- Проверьте URL в `components/FinalCTA.tsx`
- Убедитесь, что это HTTPS URL (не HTTP)

---

### Проблема 2: "server_error" или "Failed to access Google Sheets"

**Причина:** Не настроены переменные окружения в Vercel

**Решение:**
- Добавьте все переменные окружения в проект бекенда
- Передеплойте бекенд после добавления

---

### Проблема 3: Форма отправляется, но ничего не происходит

**Причина:** Проблема с обработкой ответа

**Решение:**
- Проверьте консоль на JavaScript ошибки
- Проверьте Network tab на ответ от сервера

---

### Проблема 4: CSP ошибка (Content Security Policy)

**Причина:** CSP блокирует выполнение скриптов

**Решение:**
- Обычно не критично для продакшна
- Vite автоматически обрабатывает это
- Проверьте, что сборка правильная

---

## ✅ Чеклист диагностики:

- [ ] Открыт правильный URL (фронтенд, не бекенд)
- [ ] Консоль браузера проверена на ошибки
- [ ] Network tab проверен на запросы к API
- [ ] API бекенда работает (`/api/healthz` возвращает `ok`)
- [ ] Все переменные окружения добавлены в Vercel
- [ ] Бекенд передеплоен после добавления переменных
- [ ] URL API правильный в коде

---

## 📝 Что нужно прислать для помощи:

Если проблема не решается, пришлите:

1. **Скриншот консоли** (F12 → Console) с ошибками
2. **Скриншот Network tab** с запросом к `/api/lead`
3. **Ответ от сервера** (вкладка Response в Network tab)
4. **Статус код** запроса

Это поможет быстро найти проблему!

---

**После проверки всех пунктов форма должна заработать!** 🚀

