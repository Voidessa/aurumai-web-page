# 📤 Инструкция: Push в GitHub

Коммит готов! Осталось только запушить в репозиторий.

## Вариант 1: Через терминал (рекомендуется)

Выполните команды:

```bash
cd /Users/hojiakbar/Downloads/aurumai-web-page-main
git push -u origin main
```

Если потребуется аутентификация:
- Введите ваш GitHub username
- Введите Personal Access Token (не пароль!)

**Как получить Personal Access Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен (показывается только один раз!)

## Вариант 2: Через GitHub Desktop

1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку: `/Users/hojiakbar/Downloads/aurumai-web-page-main`
4. Нажмите "Publish repository" или "Push origin"

## Вариант 3: Через VS Code / Cursor

1. Откройте Source Control (Ctrl+Shift+G / Cmd+Shift+G)
2. Нажмите на "..." → Push

---

**После успешного push:**
- Все файлы будут в репозитории
- `.env.local` НЕ будет загружен (защита настроена)
- Можно переходить к деплою на Vercel!

