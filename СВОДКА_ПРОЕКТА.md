# 📋 Полная сводка проекта - Что от вас требуется

## 🎯 Что было сделано

### ✅ Бекенд (готов)
Создан полнофункциональный Next.js API-only проект в папке `backend/`:

**API Endpoints:**
- `GET /api/healthz` - проверка работоспособности
- `POST /api/lead` - прием формы предзаписи
- `POST /api/inquiry` - прием вопросов/запросов на сотрудничество

**Функциональность:**
- ✅ Валидация данных (Zod)
- ✅ Сохранение в Google Sheets ИЛИ Supabase (на выбор)
- ✅ Уведомления в Telegram админ-чат
- ✅ Защита от спама (Cloudflare Turnstile - опционально)
- ✅ Rate limiting (Upstash Redis - опционально)
- ✅ UTM-трекинг
- ✅ Интеграция с GA4 (опционально)

**Документация:**
- Все инструкции по настройке и деплою готовы

### ✅ Фронтенд (готов)
Обновлен существующий Vite проект:

- ✅ Добавлен скрипт захвата UTM-параметров
- ✅ Форма предзаписи подключена к API бекенда
- ✅ Готов к работе после настройки URL бекенда

---

## 📌 Что от вас требуется (пошагово)

### ШАГ 1: Получить необходимые токены и ключи ⚡ ОБЯЗАТЕЛЬНО

#### 1.1 Telegram Bot (обязательно)
**Что нужно:** токен бота и ID чата для уведомлений

**Как получить:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям, создайте бота
4. Скопируйте токен (вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Chat ID:**
1. Создайте группу в Telegram
2. Добавьте вашего бота в группу (сделайте администратором)
3. Отправьте любое сообщение в группу
4. Выполните в терминале:
   ```bash
   curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
5. Найдите в ответе `"chat":{"id":-1001234567890}` - это ваш Chat ID

#### 1.2 Выбрать способ хранения данных

**Вариант A: Google Sheets** (проще для начала)
**Что нужно:** Service Account для Google Cloud

**Как получить:**
1. Зайдите на [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект
3. Включите "Google Sheets API" (APIs & Services → Enable APIs)
4. Создайте Service Account:
   - Credentials → Create Credentials → Service Account
   - Назовите аккаунт (например: `ai-course-bot`)
   - Создайте ключ (JSON) и скачайте файл
5. Откройте скачанный JSON файл, скопируйте:
   - `client_email` → это будет `GOOGLE_SA_EMAIL`
   - `private_key` → это будет `GOOGLE_SA_KEY` (сохраните кавычки и `\n`)
6. Создайте Google Sheets таблицу (или используйте существующую)
7. Поделитесь таблицей с email из `client_email` (дайте права редактора)
8. Скопируйте ID таблицы из URL: `https://docs.google.com/spreadsheets/d/{ЭТО_ID}/edit`

**Вариант B: Supabase** (для более серьезных проектов)
**Что нужно:** PostgreSQL база данных на Supabase

**Как получить:**
1. Зайдите на [Supabase](https://supabase.com)
2. Создайте новый проект
3. Дождитесь создания (1-2 минуты)
4. Settings → Database
5. Скопируйте Connection String (URI mode)
6. Замените `[YOUR-PASSWORD]` на ваш пароль из проекта

---

### ШАГ 2: Настроить переменные окружения бекенда ⚡ ОБЯЗАТЕЛЬНО

1. Перейдите в папку бекенда:
   ```bash
   cd backend
   ```

2. Создайте файл `.env.local`:
   ```bash
   cp env.example .env.local
   ```

3. Откройте `.env.local` и заполните:

**Минимальная конфигурация:**
```bash
# Режим хранения (выберите ОДИН)
CRM_TARGET=SHEETS
# или
CRM_TARGET=SUPABASE

# Telegram (ОБЯЗАТЕЛЬНО)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_ADMIN_CHAT_ID=-1001234567890
```

**Если выбрали SHEETS - добавьте:**
```bash
GOOGLE_SA_EMAIL=service-account@project-id.iam.gserviceaccount.com
GOOGLE_SA_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEETS_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
```

**Если выбрали SUPABASE - добавьте:**
```bash
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

**Опционально (рекомендуется для продакшена):**
```bash
# Защита от ботов (Cloudflare Turnstile)
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# Rate limiting (Upstash Redis)
REDIS_URL=rediss://:password@region.upstash.io:6379

# Аналитика (GA4)
GA4_MEASUREMENT_ID=G-XXXXXXXXXX
GA4_API_SECRET=xxxxxxxxxxxxx
```

---

### ШАГ 3: Установить зависимости и протестировать локально (опционально)

```bash
cd backend
npm install
npm run dev
```

Проверьте: откройте http://localhost:3000/api/healthz - должно вернуть `ok`

**Если используете Supabase:**
```bash
npx prisma migrate dev --name init
```

---

### ШАГ 4: Загрузить бекенд на GitHub ⚡ ОБЯЗАТЕЛЬНО

1. Создайте репозиторий на GitHub:
   - Зайдите на [github.com](https://github.com)
   - Нажмите "+" → "New repository"
   - Название: `ai-course-backend` (или любое другое)
   - Выберите Private (рекомендуется)
   - **НЕ** добавляйте README, .gitignore, лицензию

2. Загрузите код:
   ```bash
   cd backend
   git init
   git add .
   git commit -m "Initial commit: Next.js API backend"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/ai-course-backend.git
   git push -u origin main
   ```
   **Замените `YOUR_USERNAME` на ваш GitHub username!**

---

### ШАГ 5: Деплой бекенда на Vercel ⚡ ОБЯЗАТЕЛЬНО

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New..." → "Project"
4. Импортируйте репозиторий `ai-course-backend`
5. Framework Preset должен автоматически определиться как **Next.js**
6. **ВАЖНО:** Перед деплоем добавьте переменные окружения:
   - Нажмите "Environment Variables"
   - Добавьте **ВСЕ** переменные из вашего `.env.local`:
     - `CRM_TARGET`
     - `TELEGRAM_BOT_TOKEN`
     - `TELEGRAM_ADMIN_CHAT_ID`
     - И остальные (в зависимости от выбранного режима)
7. Нажмите "Deploy"
8. Дождитесь завершения деплоя
9. **Скопируйте URL проекта** (например: `https://ai-course-backend.vercel.app`)

**Если используете Supabase:**
После деплоя выполните миграции:
```bash
npx prisma migrate deploy
```

**Проверка:**
```bash
curl https://your-backend.vercel.app/api/healthz
```
Должно вернуть: `ok`

---

### ШАГ 6: Настроить фронтенд ⚡ ОБЯЗАТЕЛЬНО

После деплоя бекенда у вас есть URL (например: `https://ai-course-backend.vercel.app`)

**Вариант A: Через переменные окружения (рекомендуется)**

Создайте `.env.local` в **корне** фронтенд проекта (не в `backend/`):
```bash
# В корне проекта
VITE_API_BASE_URL=https://your-backend.vercel.app
```

**Вариант B: Прямое изменение кода**

Откройте `components/FinalCTA.tsx` и измените строку 4:
```typescript
const API_BASE_URL = 'https://your-backend.vercel.app';
```

---

### ШАГ 7: Тестирование ⚡ ОБЯЗАТЕЛЬНО

1. Запустите фронтенд:
   ```bash
   npm run dev
   ```

2. Откройте сайт в браузере

3. Заполните форму предзаписи и отправьте

4. Проверьте:
   - ✅ Форма отправляется без ошибок
   - ✅ В Google Sheets / Supabase появилась новая запись
   - ✅ В Telegram группе пришло уведомление с данными заявки

5. Проверьте логи в Vercel Dashboard (если что-то не работает):
   - Deployments → выберите деплой → Functions → Logs

---

## 🔄 Краткая последовательность действий

```
1. Получить токены (Telegram Bot, Google Sheets/Supabase)
   ↓
2. Заполнить backend/.env.local
   ↓
3. Загрузить backend на GitHub
   ↓
4. Деплой на Vercel + добавить переменные окружения
   ↓
5. Обновить VITE_API_BASE_URL на фронтенде
   ↓
6. Протестировать форму
```

---

## ⚙️ Опциональные настройки (можно сделать позже)

### Cloudflare Turnstile (защита от ботов)
1. [Cloudflare Dashboard](https://dash.cloudflare.com) → Turnstile
2. Создайте виджет
3. Скопируйте Site Key (для фронтенда) и Secret Key (для бекенда)
4. Добавьте `TURNSTILE_SECRET_KEY` в `.env.local` бекенда

### Upstash Redis (rate limiting)
1. [Upstash](https://upstash.com) → Create Database
2. Скопируйте REST URL
3. Добавьте `REDIS_URL` в `.env.local` бекенда

### Google Analytics 4
1. Настройте GA4 Measurement Protocol
2. Получите Measurement ID и API Secret
3. Добавьте в `.env.local` бекенда

---

## 🆘 Если что-то не работает

### Форма не отправляется
- ✅ Проверьте URL бекенда (должен быть доступен)
- ✅ Проверьте CORS (Vercel по умолчанию разрешает все источники)
- ✅ Откройте консоль браузера (F12) и посмотрите ошибки

### Данные не сохраняются
- ✅ Проверьте `CRM_TARGET` (должен быть `SHEETS` или `SUPABASE`)
- ✅ Для Sheets: проверьте, что Service Account имеет доступ к таблице
- ✅ Для Supabase: выполните миграции `npx prisma migrate deploy`

### Telegram уведомления не приходят
- ✅ Проверьте токен бота
- ✅ Убедитесь, что бот добавлен в группу и является администратором
- ✅ Проверьте формат `TELEGRAM_ADMIN_CHAT_ID` (для групп начинается с `-`)

### Ошибки в Vercel
- ✅ Проверьте все переменные окружения добавлены
- ✅ Проверьте формат переменных (особенно `GOOGLE_SA_KEY` с кавычками)
- ✅ Посмотрите логи в Vercel Dashboard → Deployments → Functions

---

## 📚 Полезные файлы

Все инструкции находятся в папке `backend/`:

- **START_HERE.md** - пошаговая инструкция с примерами
- **SETUP_CHECKLIST.md** - детальный чеклист
- **DEPLOY.md** - подробная инструкция по деплою
- **INTEGRATION.md** - интеграция с фронтендом
- **GITHUB_SETUP.md** - настройка GitHub

---

## ✅ Итоговый чеклист

- [ ] Получен Telegram Bot Token
- [ ] Получен Telegram Chat ID
- [ ] Выбран способ хранения (Sheets или Supabase)
- [ ] Настроен Service Account (для Sheets) или Supabase (для DB)
- [ ] Создан и заполнен `backend/.env.local`
- [ ] Зависимости установлены (`npm install` в backend/)
- [ ] Бекенд загружен на GitHub
- [ ] Бекенд задеплоен на Vercel
- [ ] Переменные окружения добавлены в Vercel
- [ ] Health check работает (`/api/healthz`)
- [ ] Фронтенд обновлен с URL бекенда
- [ ] Форма тестируется и работает
- [ ] Данные сохраняются
- [ ] Telegram уведомления приходят

---

**🎯 Главное:** Начните с получения токенов (ШАГ 1), затем следуйте инструкциям по порядку.

**📖 Подробности:** См. `backend/START_HERE.md` для детальных инструкций.

