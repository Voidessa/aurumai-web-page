# ✅ Быстрая проверка после Push

## Автоматический деплой

✅ **Push в GitHub автоматически запустит новый деплой на Vercel**

## Что нужно проверить в Vercel:

### 1. Откройте ваш проект на Vercel

Зайдите на [vercel.com/dashboard](https://vercel.com/dashboard) и откройте ваш проект.

### 2. Проверьте настройки проекта

**Settings → General → Root Directory**

Должно быть установлено: **`backend`**

Если нет:
- Выберите `backend` из списка
- Сохраните
- Передеплойте проект (Deployments → ... → Redeploy)

### 3. Добавьте переменные окружения (КРИТИЧНО!)

**Settings → Environment Variables**

Добавьте все эти переменные (скопируйте значения из вашего `backend/.env.local`):

```
CRM_TARGET=SHEETS

TELEGRAM_BOT_TOKEN=ваш_токен

TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id

GOOGLE_SA_EMAIL=ai-course-backend@aurum-ai-web-page.iam.gserviceaccount.com

GOOGLE_SA_KEY=-----BEGIN PRIVATE KEY-----
ваш_приватный_ключ
-----END PRIVATE KEY-----

GOOGLE_SHEETS_ID=1Fb3isrRvDdOzbCuaI0myI7sWRBC3ZcMvglRvca3GjlU
```

**⚠️ ВАЖНО для GOOGLE_SA_KEY:**
- Можно вставить многострочным текстом (как есть)
- Или одной строкой с `\n`: `-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n`

### 4. Проверьте последний деплой

После push:
- Перейдите в раздел **Deployments**
- Последний деплой должен быть в статусе "Building" или "Ready"
- Если ошибка - нажмите на деплой и посмотрите логи

### 5. Проверьте работу API

После успешного деплоя откройте:
```
https://ваш-проект.vercel.app/api/healthz
```

Должен вернуться: `ok`

---

## Если что-то не работает:

### Ошибка билда?
- Проверьте Root Directory = `backend`
- Проверьте логи билда в Vercel

### API возвращает ошибки?
- Проверьте, что все переменные окружения добавлены
- Проверьте формат `GOOGLE_SA_KEY`
- Передеплойте проект после добавления переменных

### Нужно передеплоить?
- Deployments → последний деплой → ... → **Redeploy**

---

## После успешного деплоя:

1. Скопируйте URL бекенда: `https://ваш-проект.vercel.app`
2. Обновите фронтенд:
   - Создайте `.env.local` в корне проекта (рядом с `package.json`):
     ```
     VITE_API_BASE_URL=https://ваш-проект.vercel.app
     ```
   - Или обновите `components/FinalCTA.tsx`:
     ```typescript
     const API_BASE_URL = 'https://ваш-проект.vercel.app';
     ```

Готово! 🚀

