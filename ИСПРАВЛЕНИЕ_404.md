# 🔧 Исправление 404 ошибки

## ✅ ВАЖНО: 404 на корне - это НОРМАЛЬНО!

Ваш бекенд - это **API-only проект**, который не имеет главной страницы. Это правильное поведение!

### Почему 404?

В вашем проекте есть только API роуты:
- `/api/healthz` ✅
- `/api/lead` ✅
- `/api/inquiry` ✅

Но нет файла `app/page.tsx` для корня `/`, поэтому при обращении к корню сайта возвращается 404.

---

## ✅ Как проверить, что всё работает:

### 1. Проверьте API endpoint

Откройте в браузере:

```
https://aurumai-web-page.vercel.app/api/healthz
```

**Должно вернуться:**
- Текст: `ok`
- Статус: `200 OK`  

**Если вернулся `ok` - всё работает отлично! ✅**

---

## 🔍 Если API тоже не работает:

### Проблема 1: Root Directory не настроен

1. Откройте проект на Vercel
2. Settings → General → Root Directory
3. Должно быть: `backend`
4. Если нет - установите и передеплойте

### Проблема 2: Переменные окружения не добавлены

Проверьте в Settings → Environment Variables:
- ✅ `CRM_TARGET=SHEETS`
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `TELEGRAM_ADMIN_CHAT_ID`
- ✅ `GOOGLE_SA_EMAIL`
- ✅ `GOOGLE_SA_KEY`
- ✅ `GOOGLE_SHEETS_ID`

После добавления передеплойте проект.

---

## 💡 Как сделать, чтобы корень работал (опционально)

Если хотите, чтобы корень сайта показывал что-то, а не 404:

Создайте файл `backend/app/page.tsx`:

```tsx
export default function Home() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>API Backend</h1>
      <p>Это API-only проект.</p>
      <ul>
        <li><a href="/api/healthz">/api/healthz</a></li>
        <li>/api/lead (POST)</li>
        <li>/api/inquiry (POST)</li>
      </ul>
    </div>
  );
}
```

Но это **не обязательно** - для API-only проекта 404 на корне - это нормально.

---

## ✅ Чеклист проверки:

- [ ] Открыл `/api/healthz` - вернулся `ok` ✅
- [ ] Root Directory в Vercel = `backend` ✅
- [ ] Все переменные окружения добавлены ✅
- [ ] Проект передеплоен после настройки ✅

**Если `/api/healthz` возвращает `ok` - всё отлично работает!** 🎉

---

## 📝 Резюме:

**404 на корне = нормально для API-only проекта**

**Важно:** Проверьте работу через `/api/healthz` - если там `ok`, значит всё работает правильно!

