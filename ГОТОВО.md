# ✅ Бекенд готов к настройке и деплою!

## 📁 Что было создано

### Бекенд (Next.js API-only)
- ✅ Полная структура проекта в папке `backend/`
- ✅ API роуты: `/api/healthz`, `/api/lead`, `/api/inquiry`
- ✅ Библиотеки: валидация, storage, telegram, redis, turnstile, analytics
- ✅ Prisma схема для Supabase
- ✅ Документация и инструкции

### Фронтенд (обновлен)
- ✅ Скрипт захвата UTM в `index.html`
- ✅ Компонент `FinalCTA.tsx` обновлен для работы с API

## 🚀 Быстрый старт

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
cd backend
bash commands.sh
```

Это создаст `.env.local` и установит зависимости.

### Вариант 2: Ручная настройка

Следуйте инструкциям в: **`backend/START_HERE.md`**

## 📚 Документация

Все файлы находятся в папке `backend/`:

1. **START_HERE.md** ⭐ - Начните отсюда! Пошаговая инструкция
2. **SETUP_CHECKLIST.md** - Детальный чеклист с примерами
3. **DEPLOY.md** - Подробная инструкция по деплою на Vercel
4. **INTEGRATION.md** - Интеграция с фронтендом
5. **GITHUB_SETUP.md** - Настройка GitHub репозитория
6. **README.md** - Общая документация

## ⚡ Минимальные шаги для запуска

### 1. Настройка переменных окружения

```bash
cd backend
cp env.example .env.local
# Отредактируйте .env.local
```

**Минимум для работы:**
- `CRM_TARGET=SHEETS` (или `SUPABASE`)
- `TELEGRAM_BOT_TOKEN=ваш_токен`
- `TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id`

Если выбрали SHEETS:
- `GOOGLE_SA_EMAIL=...`
- `GOOGLE_SA_KEY=...`
- `GOOGLE_SHEETS_ID=...`

Если выбрали SUPABASE:
- `DATABASE_URL=...`

### 2. Установка зависимостей

```bash
cd backend
npm install
```

### 3. Локальный тест (опционально)

```bash
npm run dev
# Проверьте: http://localhost:3000/api/healthz
```

### 4. Загрузка на GitHub

```bash
cd backend
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ai-course-backend.git
git push -u origin main
```

### 5. Деплой на Vercel

1. [vercel.com](https://vercel.com) → Import Project
2. Выберите репозиторий `ai-course-backend`
3. Добавьте все переменные из `.env.local` в Environment Variables
4. Deploy

### 6. Настройка фронтенда

Создайте `.env.local` в корне фронтенд проекта:

```bash
# В корне проекта (не в backend/)
echo "VITE_API_BASE_URL=https://your-backend.vercel.app" > .env.local
```

Или обновите напрямую в `components/FinalCTA.tsx`:
```typescript
const API_BASE_URL = 'https://your-backend.vercel.app';
```

## 🔑 Получение необходимых токенов

### Telegram Bot Token
1. [@BotFather](https://t.me/BotFather) → `/newbot`
2. Скопируйте токен

### Telegram Chat ID
1. Создайте группу, добавьте бота
2. Отправьте сообщение
3. Выполните: `curl https://api.telegram.org/bot<TOKEN>/getUpdates`
4. Найдите `"chat":{"id":-1001234567890}`

### Google Sheets (если SHEETS)
1. [Google Cloud Console](https://console.cloud.google.com)
2. Создайте проект → Enable Google Sheets API
3. Credentials → Service Account → Create Key (JSON)
4. Откройте JSON, скопируйте `client_email` и `private_key`
5. Поделитесь Google Sheets таблицей с email service account

Подробнее: см. `backend/SETUP_CHECKLIST.md` раздел "Получение необходимых токенов"

## ✅ Проверка работоспособности

После деплоя:

1. **Health check:**
   ```bash
   curl https://your-backend.vercel.app/api/healthz
   ```
   Должно вернуть: `ok`

2. **Тест формы:**
   - Откройте фронтенд
   - Заполните и отправьте форму
   - Проверьте:
     - ✅ Форма отправляется без ошибок
     - ✅ Данные появились в Sheets/DB
     - ✅ В Telegram пришло уведомление

## 🆘 Проблемы?

- Проверьте логи в Vercel Dashboard
- Убедитесь, что все переменные окружения добавлены
- Проверьте формат переменных (особенно `GOOGLE_SA_KEY` с кавычками и `\n`)

## 📞 Поддержка

Все инструкции с примерами находятся в:
- `backend/START_HERE.md` - пошаговая инструкция
- `backend/DEPLOY.md` - деплой
- `backend/SETUP_CHECKLIST.md` - детальный чеклист

---

**Готово к деплою! 🎉**

Начните с: `cd backend && bash commands.sh`

