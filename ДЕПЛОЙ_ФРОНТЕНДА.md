# 🚀 Деплой фронтенда на Vercel

## ✅ Что уже готово:

- ✅ Фронтенд полностью настроен (Vite + React)
- ✅ Все компоненты на месте
- ✅ Форма интегрирована с бекендом
- ✅ API URL настроен: `https://aurumai-web-page.vercel.app`

---

## 📋 Варианты деплоя

### Вариант 1: Отдельный проект на Vercel (РЕКОМЕНДУЕТСЯ)

Это позволит вам иметь отдельные URL для фронтенда и бекенда.

#### Шаг 1: Создайте новый проект на Vercel

1. Зайдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New..."** → **"Project"**
3. Нажмите **"Import Git Repository"**
4. Выберите тот же репозиторий: `Voidessa/aurumai-web-page`

#### Шаг 2: Настройка проекта

В настройках проекта:

1. **Project Name:** `aurumai-web-page-frontend` (или любое другое название)
2. **Root Directory:** **ОСТАВЬТЕ ПУСТЫМ** (или `/`)
   - ⚠️ ВАЖНО: НЕ указывайте `backend`, оставьте корень
3. **Framework Preset:** Выберите **"Vite"** вручную
4. **Build Command:** `npm run build`
5. **Output Directory:** `dist`

#### Шаг 3: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения
3. Получите URL: `https://aurumai-web-page-frontend.vercel.app`

---

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# В корне проекта (не в backend/)
vercel

# Следуйте инструкциям:
# - Логин (если нужно)
# - Link to existing project? No
# - Project name: aurumai-web-page-frontend
# - Directory: ./
# - Build command: npm run build
# - Output directory: dist
```

---

## 🔧 Настройка после деплоя

### 1. Обновите API URL (если нужно)

Если URL бекенда отличается, обновите в файлах:

**В `components/FinalCTA.tsx`:**
```typescript
const API_BASE_URL = 'https://ваш-бекенд-url.vercel.app';
```

**В `index.html`:**
```javascript
window.__API_BASE_URL__ = 'https://ваш-бекенд-url.vercel.app';
```

### 2. Создайте файл `.env` (опционально)

В корне проекта создайте `.env.production`:

```env
VITE_API_BASE_URL=https://aurumai-web-page.vercel.app
```

Или добавьте в Vercel:
- Settings → Environment Variables
- Key: `VITE_API_BASE_URL`
- Value: `https://aurumai-web-page.vercel.app`

---

## ✅ Проверка работы

После деплоя:

1. Откройте ваш фронтенд URL
2. Проверьте, что сайт загружается
3. Прокрутите до формы
4. Заполните и отправьте тестовую форму
5. Проверьте:
   - ✅ Появилось сообщение "Заявка отправлена!"
   - ✅ Данные появились в Google Sheets
   - ✅ Пришло уведомление в Telegram

---

## 🎨 Итоговая структура проекта

После деплоя у вас будет:

- **Фронтенд:** `https://aurumai-web-page-frontend.vercel.app` (или ваш домен)
  - Полноценный сайт с лендингом
  - Форма предзаписи
  - Все компоненты и секции

- **Бекенд:** `https://aurumai-web-page.vercel.app`
  - API endpoints (`/api/healthz`, `/api/lead`, `/api/inquiry`)
  - Сохранение в Google Sheets
  - Уведомления в Telegram

---

## 🔗 Настройка кастомного домена (опционально)

Если хотите использовать свой домен:

1. В Vercel: Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

---

## 📝 Быстрый чеклист

- [ ] Создан новый проект на Vercel для фронтенда
- [ ] Root Directory = `/` (корень, НЕ `backend`)
- [ ] Framework Preset = Vite
- [ ] Build Command = `npm run build`
- [ ] Output Directory = `dist`
- [ ] Деплой завершен успешно
- [ ] Фронтенд открывается и работает
- [ ] Форма отправляется успешно
- [ ] Данные сохраняются в Google Sheets

---

**Готово! Теперь у вас полноценный работающий сайт! 🎉**

