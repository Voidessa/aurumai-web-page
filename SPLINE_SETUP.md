# 🎨 Настройка Spline 3D компонента

## ✅ Что сделано:

1. ✅ Установлен пакет `@splinetool/react-spline`
2. ✅ Создан компонент `SplineScene.tsx`
3. ✅ Интегрирован в `Hero.tsx` (верхний блок сайта)
4. ✅ Добавлены стили для loader
5. ✅ Настроена адаптивная верстка

---

## 📝 Что нужно сделать:

### Шаг 1: Получите URL вашей Spline сцены

1. Зайдите на [spline.design](https://spline.design)
2. Создайте или откройте вашу 3D сцену
3. Нажмите **"Export"** или **"Publish"**
4. Скопируйте URL сцены (например: `https://prod.spline.design/xxx-scene.splinecode`)

### Шаг 2: Обновите URL в Hero.tsx

Откройте файл `components/Hero.tsx` и найдите строку:

```typescript
const splineSceneUrl = "https://your-spline-scene-url.splinecode"; // TODO: Замените на ваш URL
```

Замените на ваш реальный URL:

```typescript
const splineSceneUrl = "https://prod.spline.design/ваш-реальный-url.splinecode";
```

---

## 🎨 Как это выглядит:

- **Spline сцена** отображается на **фоне** Hero секции
- **Текст и кнопка** поверх 3D сцены
- **Адаптивная верстка** - на мобильных Spline слегка прозрачнее
- **Lazy loading** - сцена загружается асинхронно
- **Loader** показывается при загрузке

---

## 🔧 Настройка внешнего вида:

Вы можете изменить параметры в `Hero.tsx`:

### Изменить прозрачность Spline:

```typescript
// Текущее: opacity-50 md:opacity-60 (50% на мобильных, 60% на десктопе)
<div className="absolute inset-0 w-full h-full opacity-50 md:opacity-60 -z-10">
```

Изменьте на:
- `opacity-30` - более прозрачный
- `opacity-70` - менее прозрачный
- `opacity-100` - полностью непрозрачный

### Изменить позиционирование:

Если хотите, чтобы Spline был не на фоне, а рядом с текстом, измените структуру:

```typescript
<div className="grid md:grid-cols-2 gap-8 items-center">
  <div>{/* Spline здесь */}</div>
  <div>{/* Текст здесь */}</div>
</div>
```

### Изменить размер Hero секции:

```typescript
// Текущее: min-h-[80vh]
<section id="hero" className="relative min-h-[80vh] ...">
```

Можно изменить на:
- `min-h-[60vh]` - меньше
- `min-h-screen` - на весь экран
- `min-h-[90vh]` - почти на весь экран

---

## ✅ Проверка:

После добавления URL Spline сцены:

1. Запустите локально: `npm run dev`
2. Проверьте, что Spline загружается
3. Проверьте, что текст виден поверх Spline
4. Проверьте адаптивность на мобильных устройствах

---

## 🐛 Возможные проблемы:

### Spline не загружается:

1. Проверьте URL - должен быть полный URL `.splinecode` файла
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что сцена опубликована в Spline

### Текст не виден:

1. Увеличьте прозрачность Spline (`opacity-30` вместо `opacity-50`)
2. Добавьте backdrop-blur к тексту:
   ```typescript
   <div className="relative z-10 backdrop-blur-sm ...">
   ```

### Производительность:

Если Spline тормозит:
- Уменьшите размер области (`w-1/2` вместо `w-full`)
- Уменьшите прозрачность
- Оптимизируйте сцену в Spline (уменьшите количество объектов)

---

## 📦 Что изменено:

- ✅ `components/SplineScene.tsx` - новый компонент
- ✅ `components/Hero.tsx` - обновлен для интеграции Spline
- ✅ `index.html` - добавлены стили для loader
- ✅ `package.json` - добавлена зависимость `@splinetool/react-spline`

**Бекенд не затронут - все изменения только в фронтенде!**

---

**После добавления URL вашей Spline сцены всё будет работать!** 🚀

