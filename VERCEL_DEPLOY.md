# 🚀 Деплой на Vercel - Пошаговая инструкция

## Шаг 1: Импорт проекта в Vercel

1. Перейдите на [vercel.com](https://vercel.com) и войдите (или создайте аккаунт)
2. Нажмите **"Add New..."** → **"Project"**
3. Нажмите **"Import Git Repository"**
4. Найдите репозиторий `Voidessa/aurumai-web-page` и нажмите **"Import"**

## Шаг 2: Настройка проекта

### Важные настройки:

1. **Root Directory**: Укажите `backend`
   - В настройках проекта найдите "Root Directory"
   - Выберите `backend` из списка
   - Это важно! Бекенд находится в подпапке

2. **Framework Preset**: Должен автоматически определиться как "Next.js"
   - Если нет, выберите вручную "Next.js"

3. **Build Command**: Оставьте по умолчанию `npm run build`
   - Vercel автоматически выполнит его в папке `backend`

4. **Output Directory**: Оставьте `.next` (по умолчанию)

## Шаг 3: Добавление переменных окружения

После импорта проекта, в настройках проекта перейдите в **"Settings"** → **"Environment Variables"**

Добавьте следующие переменные (скопируйте из вашего `backend/.env.local`):

### Обязательные переменные:

```
CRM_TARGET=SHEETS
```

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```
*(Пример: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)*

```
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
```
*(Пример: `-1001234567890`)*

```
GOOGLE_SA_EMAIL=ai-course-backend@aurum-ai-web-page.iam.gserviceaccount.com
```

```
GOOGLE_SA_KEY=-----BEGIN PRIVATE KEY-----
ваш_приватный_ключ
-----END PRIVATE KEY-----
```
**⚠️ ВАЖНО**: При вставке приватного ключа в Vercel:
- Убедитесь, что сохраняются переносы строк `\n`
- Или используйте формат одной строки: `-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n`
- Vercel автоматически обработает `\n` как переносы строк

```
GOOGLE_SHEETS_ID=1Fb3isrRvDdOzbCuaI0myI7sWRBC3ZcMvglRvca3GjlU
```

### Опциональные переменные (если используете):

```
TURNSTILE_SECRET_KEY=ваш_секретный_ключ
REDIS_URL=rediss://ваш_redis_url
GA4_MEASUREMENT_ID=G-XXXXXXX
GA4_API_SECRET=ваш_секрет
```

## Шаг 4: Деплой

1. После добавления всех переменных, нажмите **"Deploy"**
2. Дождитесь завершения билда (обычно 1-3 минуты)
3. После успешного деплоя вы получите URL вида: `https://ai-course-backend-xxxxx.vercel.app`

## Шаг 5: Проверка работы

1. Откройте URL вашего деплоя
2. Добавьте `/api/healthz` в конец URL
3. Должен вернуться ответ: `ok`

Пример:
```
https://ai-course-backend-xxxxx.vercel.app/api/healthz
```

## Шаг 6: Обновление фронтенда

После получения URL бекенда, обновите фронтенд:

### Вариант А: Через переменную окружения (рекомендуется)

Создайте файл `.env.local` в корне проекта (где `package.json` фронтенда):

```env
VITE_API_BASE_URL=https://ваш-бекенд-url.vercel.app
```

### Вариант Б: Через `index.html`

Откройте `index.html` и добавьте перед закрывающим тегом `</head>`:

```html
<script>
  window.__API_BASE_URL__ = 'https://ваш-бекенд-url.vercel.app';
</script>
```

## 🔧 Возможные проблемы и решения

### Проблема: Билд падает с ошибкой

**Решение**: Убедитесь, что:
- Root Directory установлен в `backend`
- Все зависимости установлены (проверьте `package.json`)
- Prisma настроен правильно (если используете Supabase)

### Проблема: API возвращает ошибки

**Решение**: Проверьте:
- Все переменные окружения добавлены в Vercel
- `GOOGLE_SA_KEY` правильно отформатирован (с переносами строк)
- Service Account имеет доступ к Google Sheets
- Telegram бот добавлен в группу/чат

### Проблема: CORS ошибки

**Решение**: Next.js API routes на Vercel автоматически настроены для CORS. Если проблемы:
- Проверьте, что запросы идут на правильный URL
- Убедитесь, что используете HTTPS

## 📝 Полезные команды Vercel CLI (опционально)

Если хотите деплоить через CLI:

```bash
npm i -g vercel
cd backend
vercel
```

## ✅ Чеклист готовности к продакшну

- [ ] Проект задеплоен на Vercel
- [ ] Все переменные окружения добавлены
- [ ] `/api/healthz` возвращает `ok`
- [ ] Тестовая отправка формы работает
- [ ] Данные появляются в Google Sheets
- [ ] Telegram уведомления приходят
- [ ] Фронтенд обновлен с URL бекенда
- [ ] Форма на продакшн сайте работает

---

**Готово!** После деплоя ваш бекенд будет доступен по HTTPS и готов принимать запросы от фронтенда.

