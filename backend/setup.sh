#!/bin/bash

# Скрипт настройки бекенда

echo "🚀 Настройка бекенда для AI Course..."

# Создаем .env.local из примера
if [ ! -f .env.local ]; then
    echo "📝 Создаю .env.local из env.example..."
    cp env.example .env.local
    echo "✅ Файл .env.local создан. Пожалуйста, заполните переменные окружения."
else
    echo "⚠️  Файл .env.local уже существует. Пропускаю создание."
fi

# Устанавливаем зависимости
echo "📦 Устанавливаю зависимости..."
npm install

# Генерируем Prisma Client (если используется Supabase)
if [ -f "prisma/schema.prisma" ]; then
    echo "🔧 Генерирую Prisma Client..."
    npx prisma generate
fi

echo ""
echo "✅ Настройка завершена!"
echo ""
echo "📋 Следующие шаги:"
echo "1. Отредактируйте .env.local и заполните все необходимые переменные"
echo "2. Для локальной разработки: npm run dev"
echo "3. Для деплоя на Vercel следуйте инструкциям в DEPLOY.md"
echo ""

