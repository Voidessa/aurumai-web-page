# 🚀 Начните отсюда!

Пошаговая инструкция для быстрого старта.

## 📝 Шаг 1: Настройка переменных окружения

### 1.1 Создайте .env.local

```bash
cd backend
cp env.example .env.local
```

### 1.2 Заполните минимальные переменные

Откройте `.env.local` и заполните:

```bash
# Обязательно выберите один вариант:
CRM_TARGET=SHEETS
# или
CRM_TARGET=SUPABASE

# Обязательно для уведомлений:
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
```

### 1.3 Если выбрали SHEETS - добавьте:

```bash
GOOGLE_SA_EMAIL=service-account@project.iam.gserviceaccount.com
GOOGLE_SA_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEETS_ID=1AbCdEf...
```

**Как получить:** см. [SETUP_CHECKLIST.md](./SETUP_CHECKLIST.md) раздел "Получение необходимых токенов"

### 1.4 Если выбрали SUPABASE - добавьте:

```bash
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

## 📦 Шаг 2: Установка зависимостей

```bash
cd backend
npm install
```

## 🧪 Шаг 3: Локальный тест (опционально)

```bash
npm run dev
```

Откройте http://localhost:3000/api/healthz - должно вернуть `ok`

## 🐙 Шаг 4: Загрузка на GitHub

### 4.1 Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. New repository → `ai-course-backend`
3. **Не** добавляйте README, .gitignore, лицензию

### 4.2 Загрузите код

```bash
cd backend
git init
git add .
git commit -m "Initial commit: Next.js API backend"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ai-course-backend.git
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

## ☁️ Шаг 5: Деплой на Vercel

### 5.1 Импорт проекта

1. Зайдите на [vercel.com](https://vercel.com)
2. Sign in через GitHub
3. Add New → Project
4. Импортируйте `ai-course-backend`
5. Framework Preset: **Next.js** (автоматически)
6. Нажмите **Deploy** (пока без переменных)

### 5.2 Добавление переменных окружения

После первого деплоя:

1. Settings → Environment Variables
2. Добавьте **все** переменные из `.env.local`:
   - `CRM_TARGET`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_ADMIN_CHAT_ID`
   - И остальные (в зависимости от выбранного режима)

### 5.3 Повторный деплой

1. Deployments → последний деплой → **Redeploy**
2. Дождитесь завершения
3. Скопируйте URL (например: `https://ai-course-backend.vercel.app`)

### 5.4 Проверка

```bash
curl https://your-backend.vercel.app/api/healthz
```

Должно вернуть: `ok`

## 🎨 Шаг 6: Настройка фронтенда

### 6.1 Обновите URL API

В корне фронтенд проекта создайте `.env.local`:

```bash
cd ..  # вернуться в корень проекта
echo "VITE_API_BASE_URL=https://your-backend.vercel.app" > .env.local
```

**Замените `https://your-backend.vercel.app` на ваш URL из Vercel!**

### 6.2 Альтернатива: прямое изменение

Откройте `components/FinalCTA.tsx` и измените:

```typescript
const API_BASE_URL = 'https://your-backend.vercel.app';
```

## ✅ Шаг 7: Финальная проверка

1. Запустите фронтенд: `npm run dev`
2. Откройте форму предзаписи
3. Заполните и отправьте форму
4. Проверьте:
   - ✅ Форма отправляется успешно
   - ✅ Данные появились в Google Sheets / Supabase
   - ✅ В Telegram админ-чат пришло уведомление

## 📚 Дополнительные материалы

- [SETUP_CHECKLIST.md](./SETUP_CHECKLIST.md) - подробный чеклист
- [DEPLOY.md](./DEPLOY.md) - детальная инструкция по деплою
- [INTEGRATION.md](./INTEGRATION.md) - интеграция с фронтендом
- [GITHUB_SETUP.md](./GITHUB_SETUP.md) - настройка GitHub

## 🆘 Проблемы?

- Проверьте логи в Vercel Dashboard
- Убедитесь, что все переменные окружения добавлены
- Проверьте формат переменных (особенно GOOGLE_SA_KEY с кавычками и \n)

---

**Готово! 🎉** Ваш бекенд работает на Vercel и готов принимать заявки.

