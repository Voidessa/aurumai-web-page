# 📋 Инструкция: Настройка Google Sheets Service Account

## Шаг 1: Создание проекта в Google Cloud

1. Перейдите на https://console.cloud.google.com
2. Войдите в аккаунт `mr.void25.2@gmail.com`
3. Нажмите на выпадающий список проектов (сверху слева) → **"Создать проект"**
4. Название проекта: `AI Course Backend` (или любое другое)
5. Нажмите **"Создать"**
6. Дождитесь создания проекта (10-20 секунд)

## Шаг 2: Включение Google Sheets API

1. В меню слева выберите **"APIs & Services"** → **"Library"**
2. В поиске введите: `Google Sheets API`
3. Нажмите на **"Google Sheets API"**
4. Нажмите кнопку **"Enable"** (Включить)

## Шаг 3: Создание Service Account

1. В меню слева выберите **"APIs & Services"** → **"Credentials"**
2. Вверху страницы нажмите **"+ CREATE CREDENTIALS"**
3. Выберите **"Service Account"**
4. Заполните:
   - **Service account name**: `ai-course-backend` (или любое название)
   - **Service account ID**: автоматически заполнится
   - **Description** (опционально): `Service account for AI Course backend`
5. Нажмите **"CREATE AND CONTINUE"**
6. В разделе "Grant this service account access to project":
   - Роль: выберите **"Editor"** (или оставьте "Project" → "Editor")
7. Нажмите **"CONTINUE"**
8. Нажмите **"DONE"**

## Шаг 4: Создание ключа (JSON)

1. Вы вернетесь на страницу Credentials
2. Найдите созданный Service Account в списке (должен быть виден)
3. Нажмите на него (на email вида `ai-course-backend@project-id.iam.gserviceaccount.com`)
4. Перейдите на вкладку **"KEYS"**
5. Нажмите **"ADD KEY"** → **"Create new key"**
6. Выберите тип: **JSON**
7. Нажмите **"CREATE"**
8. **JSON файл автоматически скачается** - сохраните его!

## Шаг 5: Получение данных из JSON

Откройте скачанный JSON файл. Он выглядит примерно так:

```json
{
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "ai-course-backend@your-project-id.iam.gserviceaccount.com",
  ...
}
```

**Вам нужно:**
1. **GOOGLE_SA_EMAIL** = значение `client_email` из JSON
2. **GOOGLE_SA_KEY** = значение `private_key` из JSON (весь блок с `-----BEGIN PRIVATE KEY-----`)

## Шаг 6: Создание Google Sheets таблицы

1. Откройте https://sheets.google.com
2. Создайте новую таблицу (или используйте существующую)
3. Название таблицы: `AI Course Leads` (или любое)
4. В правом верхнем углу нажмите **"Настройки доступа"** (кнопка "Поделиться")
5. В поле "Добавьте пользователей или группы" вставьте:
   - Email из `client_email` из JSON файла (Service Account email)
6. Выберите права: **"Редактор"**
7. Нажмите **"Отправить"**
8. **Скопируйте ID таблицы из URL:**
   ```
   https://docs.google.com/spreadsheets/d/{ЭТО_ID}/edit
   ```
   Например, если URL: `https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz1234567890/edit`
   То ID = `1AbCdEfGhIjKlMnOpQrStUvWxYz1234567890`

## Шаг 7: Отправка данных

После выполнения всех шагов, пришлите мне:

1. **GOOGLE_SA_EMAIL**: `ai-course-backend@project-id.iam.gserviceaccount.com`
2. **GOOGLE_SA_KEY**: весь блок из `private_key` (с кавычками и \n)
3. **GOOGLE_SHEETS_ID**: ID из URL таблицы

---

**⏱ Примерное время:** 5-10 минут

**💡 Подсказка:** Если на каком-то шаге возникают проблемы, напишите - помогу!

