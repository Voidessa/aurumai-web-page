# 📝 Детальная инструкция: Добавление переменных окружения в Vercel

## Шаг 1: Откройте настройки проекта в Vercel

1. Зайдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Найдите ваш проект `aurumai-web-page` в списке
3. Нажмите на название проекта, чтобы открыть его

## Шаг 2: Перейдите в раздел Settings

В верхнем меню проекта вы увидите вкладки:
- **Overview** | **Deployments** | **Analytics** | **Settings** | ...

Нажмите на вкладку **"Settings"**

## Шаг 3: Откройте Environment Variables

В левом боковом меню (в разделе Settings) найдите:
- **General**
- **Domains**
- **Environment Variables** ← **НАЖМИТЕ СЮДА**
- **Git**
- и другие...

Нажмите на **"Environment Variables"**

## Шаг 4: Добавление переменных

Теперь вы увидите форму для добавления переменных. 

### Для каждой переменной нужно:

1. **В поле "Key"** (или "Name") введите название переменной
2. **В поле "Value"** введите значение
3. Выберите **Environment** (обычно можно оставить "Production, Preview, Development" или выбрать все)
4. Нажмите кнопку **"Save"** (или "Add")

---

## Список переменных, которые нужно добавить:

### 1. CRM_TARGET

- **Key:** `CRM_TARGET`
- **Value:** `SHEETS`
- Нажмите **Save**

---

### 2. TELEGRAM_BOT_TOKEN

- **Key:** `TELEGRAM_BOT_TOKEN`
- **Value:** Ваш токен бота (пример: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
  - Этот токен вы получили от @BotFather в Telegram
  - Формат: `числа:буквы_и_цифры`
- Нажмите **Save**

---

### 3. TELEGRAM_ADMIN_CHAT_ID

- **Key:** `TELEGRAM_ADMIN_CHAT_ID`
- **Value:** Ваш Chat ID (пример: `-1001234567890`)
  - Это ID чата/группы, куда будут приходить уведомления
  - Обычно начинается с `-100` для групп
- Нажмите **Save**

---

### 4. GOOGLE_SA_EMAIL

- **Key:** `GOOGLE_SA_EMAIL`
- **Value:** `ai-course-backend@aurum-ai-web-page.iam.gserviceaccount.com`
  - Это email Service Account из Google Cloud
- Нажмите **Save**

---

### 5. GOOGLE_SA_KEY

⚠️ **ВНИМАНИЕ! Это самая важная переменная!**

- **Key:** `GOOGLE_SA_KEY`
- **Value:** Приватный ключ из JSON файла

**Откуда взять значение:**

Откройте файл JSON с ключом (например, `aurum-ai-web-page-d90378fae551.json`). 

Найдите там поле `"private_key"`. Скопируйте **ВСЁ** значение этого поля, включая:
- `-----BEGIN PRIVATE KEY-----`
- все строки ключа
- `-----END PRIVATE KEY-----`

**Как вставить в Vercel:**

**Вариант А (рекомендуется):**
- Скопируйте весь ключ как есть (многострочный)
- Вставьте в поле "Value" в Vercel
- Vercel автоматически сохранит переносы строк

**Вариант Б (если многострочный не работает):**
- Преобразуйте все переносы строк в `\n`
- Вставьте одной строкой
- Пример: `-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n`

**Важно:**
- Не удаляйте кавычки, если они есть в JSON
- Сохраните все переносы строк
- Проверьте, что ключ начинается с `-----BEGIN` и заканчивается `-----END`

- Нажмите **Save**

---

### 6. GOOGLE_SHEETS_ID

- **Key:** `GOOGLE_SHEETS_ID`
- **Value:** `1Fb3isrRvDdOzbCuaI0myI7sWRBC3ZcMvglRvca3GjlU`
  - Это ID вашей Google таблицы (из URL)
- Нажмите **Save**

---

## Шаг 5: Проверка

После добавления всех переменных вы должны увидеть список всех переменных в таблице:

| Key | Value | Environment | Actions |
|-----|-------|-------------|---------|
| CRM_TARGET | SHEETS | Production, Preview, Development | ✏️ 🗑️ |
| TELEGRAM_BOT_TOKEN | 123456... | Production, Preview, Development | ✏️ 🗑️ |
| ... | ... | ... | ... |

**Должно быть 6 переменных:**

✅ CRM_TARGET  
✅ TELEGRAM_BOT_TOKEN  
✅ TELEGRAM_ADMIN_CHAT_ID  
✅ GOOGLE_SA_EMAIL  
✅ GOOGLE_SA_KEY  
✅ GOOGLE_SHEETS_ID  

## Шаг 6: Передеплой проекта

После добавления переменных **ОБЯЗАТЕЛЬНО** передеплойте проект:

1. Перейдите во вкладку **"Deployments"**
2. Найдите последний деплой (самый верхний в списке)
3. Нажмите на три точки **"..."** справа от деплоя
4. Выберите **"Redeploy"**
5. Подтвердите **"Redeploy"**

**Или проще:**
- После добавления переменных внизу страницы Environment Variables будет кнопка **"Redeploy"** — нажмите её

---

## Где найти значения переменных локально?

Если забыли значения, они хранятся в файле:
- `backend/.env.local` (локально на вашем компьютере)

⚠️ Этот файл не загружен в GitHub (он в .gitignore), поэтому значения нужно скопировать вручную.

---

## Возможные проблемы:

### Проблема: "Invalid key format" для GOOGLE_SA_KEY

**Решение:** 
- Убедитесь, что ключ начинается с `-----BEGIN PRIVATE KEY-----`
- Проверьте, что все переносы строк сохранены
- Попробуйте вариант с `\n` вместо переносов строк

### Проблема: Переменные не работают после деплоя

**Решение:**
- Обязательно передеплойте проект после добавления переменных
- Проверьте, что Environment выбран правильно (Production, Preview, Development)

### Проблема: Не знаю, где взять TELEGRAM_BOT_TOKEN

**Решение:**
- Напишите @BotFather в Telegram
- Команда: `/mybots`
- Выберите вашего бота
- Нажмите "API Token"

---

Готово! После добавления всех переменных и передеплоя ваш бекенд будет работать. 🚀

