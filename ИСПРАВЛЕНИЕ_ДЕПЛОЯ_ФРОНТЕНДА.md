# 🔧 Исправление ошибки деплоя фронтенда

## ❌ Ошибка:

```
Error: No Next.js version detected. Make sure your package.json has "next" in either "dependencies" or "devDependencies".
```

**Причина:** Vercel автоматически определил проект как Next.js, хотя это Vite проект.

---

## ✅ Решение:

### Шаг 1: Проверьте настройки проекта на Vercel

1. Зайдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Откройте проект **фронтенда** (не бекенда!)
3. Перейдите в **Settings → General**

### Шаг 2: Исправьте настройки

Убедитесь, что настройки следующие:

#### Framework Preset:
- ❌ НЕ должно быть "Next.js"
- ✅ Должно быть: **"Vite"** или **"Other"**

Если "Other":
- Framework: `Vite`
- Build Command: `npm run build`
- Output Directory: `dist`
- Install Command: `npm install`

#### Root Directory:
- ❌ НЕ должно быть: `backend`
- ✅ Должно быть: **ПУСТО** (корень проекта)

### Шаг 3: Передеплойте

1. Сохраните изменения
2. Перейдите в **Deployments**
3. Нажмите на последний деплой
4. Нажмите **"Redeploy"** или создайте новый коммит и запушьте

---

## 🔍 Альтернативное решение через vercel.json

Если настройки в UI не помогают, создайте/обновите `vercel.json` в корне проекта:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Затем запушьте изменения:
```bash
git add vercel.json
git commit -m "Fix Vercel config for Vite frontend"
git push
```

---

## ✅ Правильные настройки проекта на Vercel:

| Параметр | Значение |
|----------|----------|
| **Root Directory** | *(пусто)* или `/` |
| **Framework Preset** | `Vite` |
| **Build Command** | `npm run build` |
| **Output Directory** | `dist` |
| **Install Command** | `npm install` |

---

## 📝 Проверка:

После исправления настройки и передеплоя:

1. Деплой должен пройти успешно
2. URL фронтенда должен открываться
3. Сайт должен загружаться без ошибок

---

**После исправления настройки проект задеплоится автоматически!** 🚀

